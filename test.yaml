    # ========
foo: 123
bar:
  one: qwerty
  two: "the secret value is ${{ secret.value }}"
  three: 3
qwqw.yaml: |+
  # comment 1
  nnn: wow! its {{ .go_template.in_unquoted_plain_stirng.in_block_string }}!
  mmm:
    aaa: 0 # comment 2
    bbb: 1
asas.json: >
  {
    "yo": "hi",
    "haha": [
      "777",
      "666"
    ],
    "content": "{{ .content.text }}"
  }
zxzx.js: |-
  var test = "a string", num = 10;
  for (let i = 0; i < num; i += 1) {
    console.log(`Hello world! ${test}`);
  }
config.ini: |
  protocol=http
  name=my-page
