foo: 123
bar:
  one: qwerty
  two: "the secret value is ${{ secret.value }}"
  three: 3
mlml.yaml: |+
  lll: wow! its {{ .go_template.in_unquoted_plain_stirng.in_block_string }}!
  mmm:
    aaa: 0
    yyy: 1
onon.json: >
  {
    "yo": "hi",
    "haha": [
      "777",
      "666"
    ],
    "content": "{{ .content.text }}"
  }
ptpt.js: |-
  var test = "a string", tn = 10;
  for (let i = 0; i < n; i += 1) {
    console.log(`Hello world! ${test}`);
  }
config.ini: |
  protocol=http
  name=my-page
